import 'dart:ui';
import 'package:flutter/cupertino.dart';

const EdgeInsets top2 = const EdgeInsets.only(
  top: 2,
);

const EdgeInsets top16 = const EdgeInsets.only(
  top: 16,
);

const EdgeInsets left8 = const EdgeInsets.only(
  left: 8,
);

const EdgeInsets left16Top16 = const EdgeInsets.only(
  left: 16,
  top: 16,
);

const EdgeInsets all16 = const EdgeInsets.all(16);

const EdgeInsets left16Top64Right16 = const EdgeInsets.only(
  left: 16,
  top: 64,
  right: 16,
);

BorderRadius standardWidgetTopCircleBorder = BorderRadius.only(
  topLeft: Radius.circular(16),
  topRight: Radius.circular(16),
);

const BorderRadius radTopLett16TopRight16 = const BorderRadius.only(
  topLeft: Radius.circular(16),
  topRight: Radius.circular(16),
);

const BorderRadius radBotLeft16BotRight16 = const BorderRadius.only(
  bottomLeft: Radius.circular(16),
  bottomRight: Radius.circular(16),
);

const BorderRadius radAll16 = const BorderRadius.only(
  bottomLeft: Radius.circular(16),
  bottomRight: Radius.circular(16),
  topLeft: Radius.circular(16),
  topRight: Radius.circular(16),
);
